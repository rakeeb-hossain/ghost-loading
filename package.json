{
  "name": "ghost-loading",
  "version": "1.0.0",
  "description": "Synchronized ghost loading components",
  "main": "index.js",
  "workspaces": [
    "examples/*",
    "packages/*"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
